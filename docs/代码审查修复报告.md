# 代码审查与修复报告

> 审查时间：2026-08-01 ~ 2026-08-02
> 范围：Argus RAG 平台（FastAPI 后端 + Vue 3 前端）
> 状态：持续维护，本文档记录已发现并修复的问题

---

## 一、修复记录总览

### 2026-08-13（正确性/数据一致性专项修复）

| # | 问题 | 类型 | 修复方式 | 影响文件 |
|---|---|---|---|---|
| 30 | **重传/重试不清旧索引**：re-chunk 只删 DB chunks，旧向量（按旧 chunk_id 存）与旧 ES 文档残留为幽灵数据抢占 top_k | Bug | 重建前无条件清理该文档向量与 ES（被杀的 worker 可能留下"向量已写、chunks 已回滚"的幽灵，chunk 计数检测会漏）；chunks 落库后先 commit 再写向量/ES（崩溃不再产生指向不可见行的孤儿向量） | `ingestion/pipeline.py` |
| 31 | **邻居块扩展死代码且跨文档**：只取 ranked 的 chunk_id，±1 永远不在结果集；chunk_id 全局自增 ±1 会跨文档取块 | Bug | 按 (document_id, chunk_index) 窗口一次 SQL 取邻居（`tuple_().in_()`），附带 group_id 过滤与软删排除 | `qa/retrieval.py` |
| 32 | **ingestion 任务崩溃后永久卡 RUNNING**：无心跳/启动恢复，文档也停留在 PROCESSING | Bug | 新增 `recover_interrupted_jobs()`：启动时（worker 启动前）把遗留 RUNNING 任务与对应文档标记 FAILED（"任务中断：服务重启，请重试"），可从 UI 重试 | `ingestion/job_service.py`、`main.py` |
| 33 | **QA 流客户端断开跳过落库/用量**：GeneratorExit 直接穿透，无 try/finally；持久化/用量失败静默吞掉 | Bug | ask_stream 生成循环包 try/finally：断连/LLM 异常均落库（部分答案也保留）并记录用量（失败 success=False）；`_persist_session`/`_record_usage` 失败改 `logger.exception` | `qa/service.py` |
| 34 | **并发审批竞态 500 + group_id 被忽略**：双击审批/接受邀请触发成员唯一约束 500；approve/reject 不校验路径 group_id 归属 | Bug | `process_request`/`respond_invitation` 行加 `with_for_update` 串行化（第二请求等锁后看到新状态返回"已被处理"）；`process_request` 增加 group_id 参数并校验归属；路由透传 | `group/service.py`、`group/router.py` |
| 35 | **流式引用与 CITATIONS 编号脱节**：流式把全部证据当引用，非流式按 LLM 编号过滤 | Bug | 流式解析 `<<<CITATIONS>>>` 编号后按编号过滤（与非流式一致），LLM 未引用则返回空引用 | `qa/service.py` |
| 36 | **新库首次 QA 500**：pgvector 表/HNSW/GIN 索引只在 ingestion 时惰性创建 | Bug | `ensure_table()` 移到启动 `_init_database`（幂等），pipeline 中移除惰性调用 | `main.py`、`ingestion/pipeline.py` |
| 37 | **MinIO 同步调用阻塞事件循环**：大文件上传/下载/合成期间所有请求（含 SSE）卡死 | 性能 | 存储服务全量异步化：同步 minio-py 调用包 `asyncio.to_thread`；新增 `check_health()`；全部 9 处调用点改 await（含健康检查） | `engine/storage.py`、`document/service.py`、`document/maintenance.py`、`ingestion/pipeline.py`、`system/router.py` |
| 38 | **total_calls 语义错误 + 用户时间序列化不一致**：overview 的 total_calls 实为今日调用数；list_users 带 "Z" 而 get_user 不带 | Bug | total_calls 改全量 count；get_user 时间戳补 "Z"（与全局约定一致） | `metrics/service.py`、`user/service.py` |

### 2026-08-05（会话期间已修复）

| # | 问题 | 类型 | 修复方式 | 影响文件 |
|---|---|---|---|---|
| 28 | **助手知识库检索回复流式中途消失**（"闪一下"没回复，刷新后完整回复出现）：KB 工具返回的 citations 是 snake_case `file_name`，前端 `AssistantCitationBar` 直接调 `c.fileName.toLowerCase()` → TypeError → Vue 卸载整个消息气泡 | Bug（已复现验证） | ① 工具返回完整 camelCase 引用结构（`documentId/chunkId/chunkIndex/fileName/score/snippet`），`EvidenceDocument` 补 `document_id/chunk_id`；② 引用条 `fileName` 空值保护；③ `list_messages`/`get_context` 序列化 `structured_payload`（此前存了不返回，刷新后引用永不显示）；④ `toUi` 解析 citations（兼容 JSON 列对象/字符串两种形态） | `assistant/agent/tools.py`、`qa/retrieval.py`、`assistant/service.py`、`AssistantCitationBar.vue`、`AssistantView.vue`、`types/assistant.ts` |
| 29 | **旧格式引用点击 422**：修复前存的 citations 无 `documentId`（`undefined` ≠ `null` 判断漏掉）→ 点击请求 `/documents/undefined/download` → FastAPI 422；且引用点击打开整文档预览而非相关分块 | Bug/体验 | ① 引用条改为 QA 同款宽卡片（文件名+相似度条+片段号，横向滚动一行一块）；② 点击引用改开新 `ChunkPreviewModal`：拉取该文档分块列表，高亮定位被引用分块（含"引用来源"标记），可滚动浏览全部分块；③ `documentId == null`（覆盖 undefined）→ 旧引用显示但置灰不可点；④ QA `CitationRail`/`QaView` 同类判断一并加固 | `AssistantCitationBar.vue`（重写）、`components/ChunkPreviewModal.vue`（新建）、`AssistantView.vue`、`CitationRail.vue`、`QaView.vue` |

### 2026-08-02（本轮 Review 修复）

| # | 问题 | 类型 | 修复方式 | 影响文件 |
|---|---|---|---|---|
| 24 | `UserContext` 写了没人读（contextvar 存了用户但无消费点） | 死代码/功能缺失 | 接入 `LoggingMiddleware`——access 日志现在记录 `userId=`，排障可知操作者 | `common/middleware/logging.py` |
| 25 | 群组接口时间格式缺时区后缀（`isoformat()` 无 `Z`）——浏览器按本地时区解析，差 8 小时 | Bug | `group/service.py` 的 `_fmt` 补 `+ "Z"`，与其他模块一致 | `group/service.py` |
| 26 | 文档删除时向量/ES/MinIO 清理失败仅打 warning 被吞——残留向量会再次积累（曾实际踩坑） | Bug/风险 | 失败改记 **error 级别**（含异常信息）；`delete()` 返回各步骤清理状态，接口返回时提示"部分索引清理失败" | `document/service.py`、`document/router.py` |
| 27 | `auth/dependencies.py` 白名单死分支：白名单路径不经过 `get_current_user`，该 `if` 永不执行且语义错误（白名单应放行而非 401） | 死代码 | 删除 `WHITELIST_PATHS` 及死分支 | `auth/dependencies.py` |

### 2026-08-01（会话期间已修复）

| # | 问题 | 类型 | 修复方式 | 影响文件 |
|---|---|---|---|---|
| 1 | **群组授权缺口（IDOR）**：document/qa/assistant 路由只校验登录、不校验群组成员身份，任意用户可传任意 groupId 读写他人群组文档 | 安全漏洞 | 新增 `require_group_access()`（成员校验 + 群组状态检查），全部相关路由接入；上传会话补归属校验 | `group/service.py`、`document/router.py`、`qa/router.py`、`assistant/router.py` |
| 2 | `.env.example` 提交了真实 API Key（DeepSeek + DashScope） | 密钥泄露 | 替换为占位符；**注意：密钥仍在 git 历史中，需到控制台轮换** | `.env.example` |
| 3 | QA `stream-ask` 非真流式：后端收完整响应后才一次性发 `answer` 事件 | 功能缺陷 | 新增 `StreamingAnswerParser` 流式分隔符解析器，逐 token 推送 `token` 事件 | `qa/service.py` |
| 4 | `vector_store._embed_texts` 无意义死代码块 | 死代码 | 删除 | `engine/vector_store.py` |
| 5 | 用量统计定价表缺 deepseek；`model_name` 记的是 .env 默认而非实际激活模型 | Bug | 补 deepseek-chat/reasoner 定价；`_record_usage` 传实际模型名 | `metrics/collector.py`、`qa/service.py` |
| 6 | 助手自动标题硬编码读 admin(user_id=1) 的模型配置 | Bug | `_auto_title_async` 改为传当前用户，删冗余包装方法 | `assistant/service.py` |
| 7 | RRF K=0 排序激进；融合后 top_k=5 召回不足；`top_score>=0.85` 归一化后恒真（死条件） | 效果/死代码 | RRF_K→60；FUSION_TOP_K=8；移除恒真条件 | `qa/retrieval.py` |
| 8 | 查询规划用 .env 默认模型，忽略管理员激活配置 | 效果 | `plan()` 按用户取激活模型，失败回退 .env | `qa/query_planning.py` |
| 9 | 检索双通道/多查询串行执行 | 性能 | `asyncio.gather` 全并行（查询 × 双通道） | `qa/retrieval.py` |
| 10 | 流式版证据 NONE 不短路，白调 LLM（实测浪费 7.4s） | 性能 | NONE 直接发拒答事件跳过 LLM；前端透传 reasonCode/reasonMessage | `qa/service.py`、`api/qa.ts`、`QaView.vue` |
| 11 | 前端 token 事件未解析 JSON——流式显示 `{"text":"..."}` 原文 | Bug | `dispatchQaSseEvent` 的 token 分支 JSON.parse 取 text | `api/qa.ts` |
| 12 | 后端 `parser.flush()` 尾部不经过 token 事件——最后几个字跳变 | Bug | flush 结果作为 token 事件发出 | `qa/service.py` |
| 13 | 调用趋势图双系列共用一个 Y 轴，Token 量级压扁调用量折线 | Bug | 双 Y 轴归一化（左轴调用量、右轴 Token） | `MetricsView.vue` |
| 14 | 趋势数据不补零，只有有记录的天——折线断裂/单点不渲染 | Bug | `daily_trend` 固定返回 30 天补零 | `metrics/service.py` |
| 15 | 数学公式不渲染（LaTeX 原样文本显示） | 功能缺陷 | 引入 KaTeX，新建 `utils/markdown.ts` 统一渲染（marked + KaTeX + DOMPurify），三处渲染点接入 | `utils/markdown.ts`、QaMessage/AssistantMessage/DocumentPreviewModal |
| 16 | **文档删除向量清理 SQL 类型错误**：`cmetadata->>'document_id' IN (1)` text vs integer，每次删除静默失败 → 残留 118 条向量 | Bug | SQL 加引号；清理已删文档的残留向量 | `engine/vector_store.py` |
| 17 | 管理页统计接口 `func.case(...)` 用法错误 → 500 | Bug | 改用 `case((cond, val), else_=...)` | `document/router.py` |
| 18 | 重试成功文档残留历史 `failure_reason`（管理页错误信息挂在 READY 文档下） | Bug | pipeline 成功时清空 failure_reason；前端仅 FAILED 显示；清理存量数据 | `ingestion/pipeline.py`、`AdminDocumentsView.vue` |
| 19 | `el-select` 空字符串 value 显示英文 "Select" | Bug | "全部状态" value 改 `'ALL'` + 显式 placeholder | `AdminDocumentsView.vue` |
| 20 | QA 会话不落库（前端内存，刷新即丢）、无管理视图 | 功能缺失 | 独立 `qa_sessions`/`qa_messages` 表落库（4 条路径）；管理问答历史页；后续补云端历史续聊 | `qa/models.py`、`qa/service.py`、`qa/history_service.py` 等 |
| 21 | Element Plus 默认英文（"Total 0"） | 体验 | 配置 `zh-cn` locale | `main.ts` |
| 22 | 管理问答历史 hover 不显示全文（后端截断 150 字 + tooltip 失效） | Bug | 后端返回完整文本；自研 `HoverTextPreview` 悬浮弹层（fixed 定位，绝对可靠） | `qa/history_service.py`、`components/HoverTextPreview.vue` |
| 23 | 管理控制台阶段 1-4（QA 落库、全局文档/群组、问答历史、数据洞察、系统健康、审计日志、存储清理、用户创建与用量） | 功能 | 见各提交 | 多文件 |
| 24 | 助手/QA 会话记忆可视化、消息分页、会话归档（记忆 LLM 语义摘要 + 游标分页 + 归档/恢复/惰性自动归档） | 功能 | 见各提交 | `assistant/*`、`metrics/*` 等 |

---

## 二、本轮四项修复详情

### 修复 24：UserContext 接入日志中间件

- **问题**：`UserContext`（contextvars）在认证时 `set(user)`，但全项目无 `get()` 消费点——"写了没人读"；同时 access 日志缺少操作者信息。
- **原因**：项目采用显式传参架构（`Depends(get_current_user)` 显式传给 service），contextvar 通道闲置。
- **修复**：`LoggingMiddleware` 在 `call_next` 之后读取 `UserContext.get()`（此时依赖已执行），access 日志追加 `userId=` 字段。
- **效果**：`2026-08-02 ... POST /api/qa/ask -> 200 (123ms) userId=1`——排障可直接定位操作者。

### 修复 25：群组接口时间格式缺时区后缀

- **问题**：`group/service.py` 的 `_fmt` 输出 `isoformat()`（无 `Z`），而 assistant/qa/audit 模块均有 `+ "Z"`。浏览器 `new Date()` 解析无后缀时间按**本地时区**，显示会差 8 小时。
- **修复**：`_fmt` 补 `+ "Z"`，全库时间序列化统一。

### 修复 26：文档删除清理失败的可见性

- **问题**：`DocumentQueryService.delete` 的向量/ES/MinIO 清理失败只打 `logger.warning` 且不包含异常信息——失败静默，残留向量会重新出现在检索结果（此前已因同类问题产生 118 条残留）。
- **修复**：
  1. 失败改记 **`logger.error`**（带 document_id 与异常详情）；
  2. `delete()` 返回 `{"vector": bool, "es": bool, "storage": bool}` 清理状态；
  3. 用户/管理员删除接口在任一清理失败时返回提示："文档已删除，但部分索引清理失败（向量/搜索/存储）…"。

### 修复 27：删除白名单死分支

- **问题**：`get_current_user` 中的 `WHITELIST_PATHS` 判断——login/register/refresh/logout 这些路由**不依赖** `get_current_user`，该分支永不执行；且语义错误（白名单应放行，却抛 401）。
- **修复**：删除 `WHITELIST_PATHS` 常量与死分支。

---

## 三、遗留事项（未修复，供后续跟进）

| 项 | 说明 | 优先级 |
|---|---|---|
| 非流式 `ask()` 证据 NONE 时仍先调 LLM 再丢弃 | 流式版已短路，非流式版未加（`qa/service.py`） | 中 |
| 未知模型成本按 qwen-plus 默认价计算 | `metrics/collector.py` 的 `PRICING.get` fallback | 低 |
| `datetime.utcnow()` 混用（deprecation） | `auth/security.py` RefreshTokenService | 低 |
| 死代码：`CitationAssembler`（qa/citation.py 整文件）、`qa/router.py`/`auth/router.py` 未使用 import、`ResultHolder.thinking` 空壳、前端 `fetchTrend`/`fetchAssistantSessionDetail` 无调用 | 建议清理 | 低 |
| 重复代码：`_fmt`（4 处）、前端 `formatTime/formatSize/formatRelative`（6+ 处）、管理页表格骨架（4 页同构） | 可提取公共工具/组件 | 低 |
| 审计未覆盖用户侧操作（退组、移除成员） | 按需补充 | 低 |
| `get_chat_config` 每次调用开新 DB session（单请求 2-3 次） | 可加请求级缓存 | 低 |
| QA 前端"本地会话"（纯内存）与"云端历史"（一问一答一条）双轨割裂 | 建议以云端会话分组统一 | 架构 |

---

## 四、2026-08-03 深度代码审查（三代理并行）

> 审查方式：3 个审查代理并行深度阅读（后端核心 / 后端辅助 / 前端），交叉验证。
> 状态：全部未修复，待排期。

### 🔴 严重（安全漏洞 / 崩溃）

| # | 问题 | 位置 |
|---|---|---|
| 1 | **IDOR 越权**：`list_messages`/`get_context` 只按 session_id 查库无归属校验，任意登录用户可枚举他人会话读取内容；`chat/chat_stream` 可向他人会话写入消息 | `assistant/router.py` + `service.py` |
| 2 | **越权检索**：`knowledge_base_search` 工具不校验群组权限，KB_SEARCH 模式可传任意 groupId 读取他人群组文档 | `assistant/agent/tools.py` |
| 3 | **成员列表泄露**：`GET /groups/{id}/members` 无权限校验，任意用户可看任意群组成员（含 email） | `group/router.py:47` |
| 4 | **500 崩溃**：非流式 `chat` + ADMIN 写工具触发 GraphInterrupt 冒泡 → 500；非流式路由缺 ADMIN 角色校验 | `assistant/facade.py` + `router.py` |
| 5 | **token 吊销缺口**：用户被禁用/降权后旧 access token 仍有效 30 分钟（JWT 内 role 直接可信） | `auth/dependencies.py` |
| 6 | **账号 token 明文存储**：localStorage 存最多 5 个账号 accessToken，一次 XSS 全丢 | `stores/auth.ts` |
| 7 | `create_invitation` 不校验 invitee 存在 → FK 500；管理员可禁用自己锁死入口 | `group/service.py` + `user` |

### 🟠 中（功能 bug）

| # | 问题 | 位置 |
|---|---|---|
| 8 | **助手引用栏永不显示**：SSE `done` 事件 reply/citations 为空，助手回答的引用证据永远不出现 | assistant 全链路 |
| 9 | QA `onError` 被兜底块覆盖：错误信息被清空、已有内容时 answered 误置 true | `QaView.vue:236` |
| 10 | 助手流意外断开时 `streaming` 永不清除，光标一直闪 | `AssistantView.vue` |
| 11 | `handleRetry` 重试会重复 push 用户消息（后端多记一轮） | `AssistantView.vue:502` |
| 12 | 文档列表轮询无 `immediate`：进页面时已有 PROCESSING 文档不启动轮询 | `DocumentListView.vue:256` |
| 13 | **前端 vue-tsc 处于报错状态**（SettingsView 类型等 12+ 处 TS 错误），`npm run build` type-check 过不了 | SettingsView / QaView 等 |
| 14 | `complete_upload` 先合并文件再查重（重复上传浪费存储）；分片无唯一约束、hash 不校验 | `document/service.py` |
| 15 | pipeline 失败不清理旧数据：重试时旧 chunks/向量残留 | `ingestion/pipeline.py` |
| 16 | 测试连接 401/404 误报"连接成功"（`< 500` 判定） | `models_config/service.py:117` |
| 17 | 密码 72 字节限制不一致（register 不检查、login 检查） | `auth/service.py` |
| 18 | 时区不一致：`user get_user`、`document _fmt` 缺 "Z" 后缀 | user/document service |
| 19 | `reindex_es` 不删旧 ES 记录，chunk ID 变化时残留陈旧文档 | `reindex_es.py` |
| 20 | QA 非流式 `ask` 的 user_msg 缺 `evidence_guidance` 字段（与流式路径提示词漂移） | `qa/service.py` |
| 21 | 流式 delta 去重 `startswith(full_reply)` 可能整段丢弃（应取共同前缀） | `assistant/agent/facade.py:121` |
| 22 | 重试 `next_retry_at` 在 RUNNING/SUCCESS 时不清空 | `ingestion/job_service.py` |

### 🟡 死代码（约 20 处）

- `facade.chat_sync` 无调用方、`qa/citation.py` 整模块、`session_memory` 5 列只读不写、`ResultHolder.thinking` 恒空
- group/user/assistant schemas 大半未用（路由全用 `body: dict`）、`update_group`/`delete_group` 无路由
- 前端 `fetchPlatformStats`/`resetPasswordByIdentity`/`sessionMode`/`escapeHtml`/`historyOpen` 未使用
- router `meta.requireAdmin` 从未被守卫读取；`factory.py` create_agent 返回的 `tools` 未用

### 🟠 性能

- **PG 无 HNSW 向量索引**（CLAUDE.md 声称有，实际未建）→ 向量检索全表扫描；`cmetadata->>'group_id'` 无索引
- QA 历史列表 N+1（每会话一次消息查询）
- `get_chat_config` 每轮对话开 3-4 次 DB session；MemorySaver 内存无界增长；`search_knowledge` 逐群组串行
- 前端消息渲染 O(n²)（每个 token 全量重渲 markdown/KaTeX）
- 每请求新建 httpx client 无连接复用（embedding）

### 建议修复顺序

1. #1-4 安全漏洞 + 崩溃（必须）
2. #8/#13 功能缺失 + 构建报错
3. #5-7 安全加固
4. 死代码清理
5. 性能（HNSW 索引、N+1、配置缓存）
